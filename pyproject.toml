[project]
name = "release-puller"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = ["pyfangs"]

[tool.uv.sources]
pyfangs = { git = "ssh://git@github.com/leandrorojas/pyfangs", rev = "v0.7.2" }
